[package]
name = "card_shuffling"
version = "0.1.0"
edition = "2021"
description = "A library for building, shuffling, and evaluating Uno No Mercy card decks."
license = "MIT"
keywords = ["uno", "card", "shuffle", "game"]
categories = ["game-engines"]

[dependencies]

[features]
grind = []
